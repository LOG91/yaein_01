<!DOCTYPE html>
<html lang="en">

<head>
    <% include ./head.ejs %>
</head>

<body>
    <% include ./header.ejs %>
        <div class="col-md-6" style="float:left;">
            <div class="list-group">
                <div href="#" class="list-group-item gender">
                    남자
                </div>
                <% for (var i = 0; i < results.length; i ++) {
                    var curName = results[i]._doc.cellName; %>
                    <div class="list-group-item cell" data-toggle="collapse" href="#collapse1">
                        <%= curName %>
                    </div>
                    <% } %>

            </div>
        </div>
</body>

</html>